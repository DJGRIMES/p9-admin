import React from "react";

const Dashboard: React.FC = () => {
  return (
    <div>
      <h2>Dashboard</h2>
      <p>
        This will show a quick overview of Founts, invites, and receipts,
        plus service health once we wire it up.
      </p>
    </div>
  );
};

export default Dashboard;
